{{- if .Values.adminservice.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "apollo.adminservice.fullname" . }}
  labels:
    app: {{ template "apollo-helm.name" . }}
    chart: {{ template "apollo-helm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: "{{ .Values.adminservice.name }}"
{{- if .Values.adminservice.service.annotations }}
  annotations:
{{ toYaml .Values.adminservice.service.annotations | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.adminservice.service.type }}
  {{ if .Values.adminservice.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
    {{- range .Values.adminservice.service.loadBalancerSourceRanges }}
    - {{ . }}
    {{- end }}
  {{ end }}
  {{ if and (eq "LoadBalancer" .Values.adminservice.service.type) .Values.adminservice.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.adminservice.service.loadBalancerIP }}
  {{ end }}
  ports:
    - name: http
      port: {{ .Values.adminservice.service.servicePort }}
      protocol: TCP
      targetPort: 8090
    {{ if and (eq "NodePort" .Values.adminservice.service.type) .Values.adminservice.service.nodePort }}
      nodePort: {{ .Values.adminservice.service.nodePort}}
    {{ end }}
  selector:
    app: {{ template "apollo-helm.name" . }}
    component: "{{ .Values.adminservice.name }}"
    release: {{ .Release.Name }}
{{- end }}
